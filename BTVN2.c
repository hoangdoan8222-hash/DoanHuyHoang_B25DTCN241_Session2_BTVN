#include <stdio.h>
#include <math.h>

int main(){
	int height = 30;
	int width = 30;
	int sum = height + width;
	int minus = height - width;
	int multi = height * width;
	int div = height / width;
	int dev2 = height % width;
}
